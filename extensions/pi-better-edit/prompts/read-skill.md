Read a file as plain text — no hash anchors, no served state. Use this to load skill content (SKILL.md or any file in its directory) for invocation and reference; the content is for consumption, not editing. For files you may edit, use `read`, which returns HASH│content anchors. Images → visual attachments; binary/directory → rejected; long content is truncated like the builtin read.
